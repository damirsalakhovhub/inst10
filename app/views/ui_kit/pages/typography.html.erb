<%= render "ui_kit/shared/layout" do %>
  <main class="ui-kit-main">
    <h1>Typography</h1>
    <!-- Typography examples will go here -->

    <h2>Start simple, scale when needed. Solid Queue runs in Puma initially. Split to dedicated workers at 10K+ users. Add Redis at 100K+ if metrics justify it.</h2>
    <p>This template follows the majestic monolith philosophy: one database, one codebase, zero complexity. Everything you need, nothing you don't. YAGNI enforced.</p>


    <h3>Start simple, scale when needed. Solid Queue runs in Puma initially. Split to dedicated workers at 10K+ users. Add Redis at 100K+ if metrics justify it.</h3>


    <h4>Start simple, scale when needed. Solid Queue runs in Puma initially. Split to dedicated workers at 10K+ users. Add Redis at 100K+ if metrics justify it.</h4>


    <h5>Start simple, scale when needed. Solid Queue runs in Puma initially. Split to dedicated workers at 10K+ users. Add Redis at 100K+ if metrics justify it.</h5>


    <h6>Start simple, scale when needed. Solid Queue runs in Puma initially. Split to dedicated workers at 10K+ users. Add Redis at 100K+ if metrics justify it.</h6>









  </main>
<% end %>

