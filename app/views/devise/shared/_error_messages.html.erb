<% if resource.errors.any? %>
  <div class="auth-form-errors" data-turbo-cache="false">
    <% resource.errors.full_messages.each do |message| %>
      <p><%= message %></p>
    <% end %>
  </div>
<% end %>
