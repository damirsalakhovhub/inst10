<h1>inst10</h1>

<p class="lead">
  Production-ready Rails 8 foundation. Start building features, not infrastructure.
</p>

<div class="info-box">
  <p><strong>Rails</strong> <%= Rails.version %> • <strong>Environment</strong> <%= Rails.env.titleize %></p>
</div>

<h2>What's Included</h2>

<p>Everything you need, nothing you don't.</p>

<ul>
  <li><strong>Authentication & Authorization</strong> — Devise + Pundit, ready to use</li>
  <li><strong>Background Jobs</strong> — Solid Queue runs in Puma. No Redis needed</li>
  <li><strong>Caching</strong> — Solid Cache uses PostgreSQL. One database for everything</li>
  <li><strong>Security</strong> — Comprehensive rate limiting, CSP, CORS, security scanning</li>
  <li><strong>Testing</strong> — RSpec with examples. FactoryBot factories. CI/CD configured</li>
  <li><strong>Deployment</strong> — Kamal 2.0 for simple Docker deploys. One command ships</li>
</ul>

<h2>Philosophy</h2>

<p>
  PostgreSQL for everything. Database-backed jobs, cache, and WebSockets. One technology to master.
</p>

<p>
  Start simple, scale when needed. Solid Queue runs in Puma initially. Split to dedicated workers at 10K+ users. Add Redis at 100K+ if metrics justify it.
</p>

<h2>Ready to Build</h2>

<p>
  Configure your project name in <code>.env</code> and start building. 
  Template updated: <%= Date.current.strftime("%B %d, %Y") %>
</p>
