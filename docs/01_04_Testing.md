Testing Basics

Acceptance criteria
- Все основные страницы загружаются без ошибок.
- Формы рендерятся корректно.
- Критичные ошибки (undefined variable, routing errors) ловятся до коммита.

Jobs to be done

1. Smoke Tests (базовая проверка рендеринга)
   ◽️ Главная страница загружается (200 статус)
   ◽️ Главная страница отображает основной контент
   ◽️ Форма входа рендерится без ошибок
   ◽️ Форма регистрации рендерится без ошибок
   ◽️ Header рендерится корректно
   📦 RSpec и Capybara настроены (из коробки шаблона)

2. Authentication Flow Tests
   ◽️ Форма входа содержит все необходимые поля (email, password, remember_me)
   ◽️ Форма регистрации содержит все необходимые поля (email, password, password_confirmation)
   ◽️ Кнопки submit присутствуют на формах
   ◽️ Переменные контроллера доступны в view (@resource, @devise_mapping)
   📦 Devise test helpers доступны (настроено в rails_helper)

3. System Test Configuration
   📦 Capybara драйвер настроен (rack_test для базовых тестов)
   ◽️ Отключены автоматические скриншоты (чтобы не требовался selenium)
   📦 FactoryBot настроен для создания тестовых данных
   📦 Test database автоматически очищается между тестами

4. Test Organization
   ◽️ Тесты организованы по функционалу (spec/system/landing_page_spec.rb, spec/system/authentication_spec.rb)
   ◽️ Один файл тестов на одну страницу/функционал
   📦 RSpec структура следует Rails conventions

5. CI/CD Integration
   ⏸️ Автоматический запуск тестов перед коммитом (опционально)
   ⏸️ Тесты в CI pipeline (опционально для MVP)

6. Coverage
   ◽️ Все публичные страницы имеют smoke-тесты
   ◽️ Все формы имеют базовые тесты на рендеринг
   ⏸️ Полное покрытие функционала (отложено, достаточно smoke-тестов для MVP)

